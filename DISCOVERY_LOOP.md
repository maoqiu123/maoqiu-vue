# Rock Discovery Loop

独立的“岩石相似材料可信反馈闭环平台”，设计依据是文章 [Jeff Dean 的 Discovery Loop：AI for Science 真正稀缺的，是“可信反馈”](https://mp.weixin.qq.com/s/kE8F9ZgH8eg5CyJ6OgIJFg)。

## 平台目标

平台不是再增加一个模型问答页，而是将可重复的科研过程组织为：

> 目标 → 数据基线 → 模型生成新配方 → 固定实验流程 → 实测 → 与基线比较 → 决策

每轮目标、假设、候选、版本、观测、失败原因、证据与决策都会保存为 Research State，使研究可以恢复、复核和继续。

用户先用自然语言填写研究目标。系统调用本地 Ollama 大模型提取其中明确给出的数值性质，自动回填抗压强度、抗拉强度、泊松比、弹性模量和密度；用户核对后再生成候选配方。

## 与原系统的关系

- 独立页面入口：`/discovery-loop.html`
- 独立 Vue 组件：`src/DiscoveryLoop.vue`
- 独立 API 前缀：`/api/discovery`
- 独立 SQLite 状态：`runtime/discovery_loop.db`
- 复用原项目的真实数据集，并允许每轮选择候选配方模型
- 原系统首页仅增加“可信反馈闭环”跳转链接

## 结果审核

证据评分由三部分组成：

- 目标拟合度（45%）：新配方实测值与目标的相对偏差。
- 相对基线改善（30%）：新配方误差相对于数据基线误差缩小的比例。
- 记录完整性（25%）：执行核验、评价口径、重复实验和版本溯源。

评分和核验结果共同决定：接受候选、进入下一轮，或升级为专家复核。下一轮会排除已经探索的样本，同时保留上一轮证据和失败记录。

## 模型选择与上传

当前内置选项包括：

- `qwen3.5:9b`
- `qwen3:8b`
- `deepseek-r1:1.5b`
- `deepseek-r1:7b`
- 量子模型（QNN）

第一轮以数据集中最接近目标的基线配方为起点；第二轮及以后严格以上一轮模型生成的候选配方为起点，并结合上一轮实测结果与目标偏差继续调整。Ollama 大模型直接给出起始配方的材料比例调整，可使用抗压强度、抗拉强度、泊松比、弹性模量和密度目标。QNN 在起始配方附近筛选小幅比例调整，目前只支持抗压强度和弹性模量目标。数据集近邻只用于约束合理范围和估算性质，不会再把两条完整样本配方混合成候选。模型只负责候选配方，不参与实验流程。

模型上传文件统一保存到 `models/`：

- `.gguf`：保存后调用本机 `ollama create` 注册，成功后可作为大模型选择。
- `.pth` / `.pt`：必须包含与当前 QNN 相同结构的 `model_state_dict`，兼容检查通过后可选择。

模型文件和注册表属于本地运行数据，已从 Git 提交中排除。

## 固定实验流程与数据基线

实验流程由系统固定，不调用任何模型，也不随研究轮次改变。流程包括批次与称量记录、平行试件、养护变量、仪器记录、重复测试和异常处理。

数据基线由系统自动检索得出：根据用户设置的全部目标性质，使用与结果审核相同的平均相对误差口径，在整个数据集中寻找误差最低的已知配方。基线和候选生成所用的参考样本必须同时具有本轮全部目标性质，缺失目标数据的样本不会参与该轮比较。页面同时展示样本的完整配方以及数据集支持的全部性质，非目标性质只展示、不参与距离计算。

审核时分别计算：

- 数据基线性质与目标性质之间的综合误差。
- 新配方实测性质与目标性质之间的综合误差。
- 相对改善率：`(基线误差 - 新配方误差) / 基线误差`。

只有新配方误差更小，才能认为模型超过了数据基线。目标改变时会重新检索基线；同一研究目标的后续轮次沿用相同基线，便于横向比较。

数据基线和实验流程都不可逐轮修改，确保不同候选在同一评价流程下比较。每轮保存一份流程快照到 Research State，便于追溯。

实测反馈必须包含运行编号、环境与仪器信息、每项目标的有效试件数量，以及至少一项与目标对应的实测均值。有效试件不少于 3 个才满足重复性要求。反馈一经提交即锁定，不能覆盖；需要继续实验时应建立下一研究轮次。

验收阈值按指标区分：抗压强度相对偏差不超过 10%，抗拉强度不超过 20%，弹性模量不超过 15%，泊松比绝对偏差不超过 0.015，密度相对偏差不超过 10%。结果页逐项显示是否达标。

## 运行

```powershell
conda activate FormDesign
python -X utf8 main.py
```

另一个终端运行：

```powershell
npm run dev
```

访问现有首页后点击“可信反馈闭环”，或直接访问：

```text
http://localhost:5173/discovery-loop.html
```

## API

- `GET /api/discovery/sessions`：获取所有 Research State 轮次。
- `GET /api/discovery/models`：读取内置、Ollama 与已上传模型及其就绪状态。
- `POST /api/discovery/models/upload`：上传 GGUF 或兼容 QNN 检查点。
- `POST /api/discovery/sessions`：建立研究目标并生成第一轮候选。
- `GET /api/discovery/sessions/<id>`：读取单轮完整状态。
- `POST /api/discovery/sessions/<id>/feedback`：提交实测值和可信度核验。
- `POST /api/discovery/sessions/<id>/next`：基于当前证据生成下一轮候选。
